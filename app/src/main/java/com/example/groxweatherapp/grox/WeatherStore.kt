package com.example.groxweatherapp.grox

import com.example.groxweatherapp.model.WeatherModel
import com.groupon.grox.Store

class WeatherStore(weatherModel: WeatherModel) : Store<WeatherModel>(weatherModel)
